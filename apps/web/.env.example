# Optional: set when accessing the dev server behind a path-prefix reverse proxy
# (e.g. https://code.temp-drop-files.store/proxy/3100/...).
#
# Without this, Vite will emit absolute asset URLs like /src/styles.css which
# become https://code.temp-drop-files.store/src/styles.css and 404.
#
# VITE_BASEPATH=/proxy/3100

