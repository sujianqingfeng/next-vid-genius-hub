{
  "name": "@app/media-comments",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared timeline, layout, image inlining, and FFmpeg args for comments video rendering (local + containers).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "exports": {
    ".": {
      "browser": "./dist/index.browser.js",
      "default": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  
  "dependencies": {
    "undici": "^7.16.0"
  },
  "scripts": {
    "build": "tsdown",
    "dev": "tsdown --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "tsdown --clean"
  }
}
