# Fixtures

This folder stores large sample payloads used for local debugging and manual testing.

- Prefer putting new JSON fixtures here instead of the repo root.
- Keep secrets and personal data out of fixtures.
