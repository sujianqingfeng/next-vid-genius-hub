{
  "name": "@app/media-subtitles",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared subtitle rendering (WebVTT->ASS + FFmpeg) for local runtime and containers.",
  "main": "dist/index.mjs",
  "types": "dist/index.d.mts",
  "exports": {
    ".": {
      "import": "./dist/index.mjs",
      "types": "./dist/index.d.mts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsdown",
    "dev": "tsdown --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "tsdown --clean"
  },
  "dependencies": {
    "execa": "^9.6.1"
  }
}
